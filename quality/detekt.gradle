apply plugin: "io.gitlab.arturbosch.detekt"

detekt {
    toolVersion = "1.0.0-RC10"
    config = files("$project.rootDir/quality/rule-set.yml")
    filters = ".*test.*,.*/resources/.*,.*/tmp/.*"
    parallel = true
    input = files("src/main/kotlin", "src/main/java")
}